import styled from 'styled-components'

import ContentTable from 'components/ContentTable'

const Styled = {
  ContentTable: styled(ContentTable)`
    margin-top: 1.5625rem;
  `
}

export default Styled
